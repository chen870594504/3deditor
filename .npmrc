; GitHub Packages 的 npm registry 配置。
;
; scope 必须等于 GitHub 仓库的 owner，这是 GitHub Packages 的硬性要求，
; 所以包名是 @chen870594504/3deditor——换成别的 scope 会发布失败。
;
; 只分流 @chen870594504 这一个 scope，其余包（vue / three / pinia / @tresjs/*）
; 仍走公共源。把 global registry 整个换成 npm.pkg.github.com 的话，
; 那些公共包也全去 GitHub 找，结果是装依赖就装不动了。
;
@chen870594504:registry=https://npm.pkg.github.com

; 认证令牌不要写进这个文件——它被 git 跟踪，提交上去等于把令牌公开了。
; 放用户级的 ~/.npmrc，那份不进仓库：
;
;   //npm.pkg.github.com/:_authToken=ghp_xxxxxxxxxxxx
;
; 令牌用 classic PAT：拉包至少 read:packages，发布本包还要 write:packages。
;
; 注意：GitHub Packages **即使包是公开的，拉取也必须带令牌**，
; 不支持匿名安装。所以每个宿主项目、每台 CI 都要配一次。
;
; 包名、这个 .npmrc、以及 package.json 的 publishConfig 三处 scope 必须完全一致。
; 不一致的表现是 npm publish 悄悄发到 registry.npmjs.org 去了，而这不会报错。
