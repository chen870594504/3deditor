import type { LibraryIconPath } from '../composables/useModelLibrary'

/**
 * 「家具」「设备」两个分类的导轨图标。
 *
 * ## 它们为什么在这里
 *
 * 这两类**原先内置在 `useModelLibrary.ts` 的 `LIBRARY` 里**，现在由宿主通过
 * `SidePanel` 的 `extraSections` 传进来（宿主传的分类要自带 `icon`，不写就在导轨上
 * 退回立方体占位图）。字形一个点都没改，所以搬到这里而不是跟着分类一起删掉——
 * 写自己的 `ExtraLibrarySection[]` 时可以直接拿去当 `icon`：
 *
 *     import { ICON_FURNITURE } from './utils/libraryIcons'
 *     const MINE = [
 *       { key: 'furniture', label: '家具', kind: 'model', icon: ICON_FURNITURE, entries: [...] },
 *     ]
 *
 * 其余几个分类的图标仍然内置在 `SidePanel.vue` 的 `SECTION_ICONS` 里，跟着内置分类走
 * （那边有 `satisfies SafeIcons` 守着「新加一个内置分类却忘了画图标」）。
 *
 * ## 这两笔字形是按什么画的
 *
 * 下面每段的「凭什么不撞」是**当年**写下的，比的是当时那一整套内置图标
 * （`NAV_ICONS` 与中栏 `ModelActions`）——两笔都还在，所以那些比对仍然成立，
 * 改字形之前值得照着重读一遍。（当时还有一颗「滑轨 + 旋钮」的左栏预设图标，
 * 它随那个页面一起删掉了，几段比对里的那一条也已经去掉。）
 */

/**
 * 家具：一张侧视的椅子（靠背立柱兼后腿 + 顶梁 + 座面 + 前腿）。
 *
 * 凭什么不撞：全仓库唯一的**开口（不闭合）**字形——`floorplan` 是闭合方框、
 * `ground` 是菱形、其余都含圆或立方体；
 * 它也是唯一一个底边不闭合的（两条腿各自落地）。
 *
 * 不用桌子（横板 + 两腿与立方体族太近），也不用正面视图（两条腿加靠背在 18px
 * 下会糊成一把叉）；椅子是「家具」里最通用的那个符号，而这一类比桌子宽得多。
 */
export const ICON_FURNITURE: LibraryIconPath[] = [
  { d: 'M6.8 19.6 V5.8 H11.4 V13.4' },
  { d: 'M6.8 13.4 H16.6 V19.6' },
]

/**
 * 设备：一个插头（两根分离的插脚 + 等宽机身 + 一根引线）。
 *
 * 凭什么不撞：全仓库唯一的**带引线（弯折尾巴）**字形，也是唯一的
 * 「两根平行短插脚 + 等宽机身」。与中栏「移除」的垃圾桶有三处同时不同：
 * 垃圾桶是通栏盖线 + 收腰 + 体内两条竖线，这里是分离插脚 + 等宽机身 + 体下一根引线。
 * （两者也不会同屏同列：垃圾桶在中栏视口浮层，这个在左栏导轨。）
 *
 * 不用齿轮：经典画法是「圆 + 8 道短射线」，直接与 `NAV_ICONS.sun` 撞形。
 * 不用显示器：「横矩形 + 支架」与同一条导轨上「地板」那两块横条太近，互相拉低辨识度。
 *
 * 注意两处 `A` 的 sweep 方向是**刻意选的**、也最容易手误写反：机身上下两个圆角
 * 用 `sweep=1`（向下凸），引线拐弯用 `sweep=0`（向右拐）。引线那段的圆心落在
 * (14.8, 16.4)，终点 (14.8, 19.2) 在圆心正南——切线恰好水平，后面接 `H16.4`
 * 才是平滑的。改完必须目视。
 */
export const ICON_EQUIPMENT: LibraryIconPath[] = [
  { d: 'M9.4 3.8 V7.6 M14.6 3.8 V7.6' },
  { d: 'M6.8 7.6 H17.2 V12 A2.6 2.6 0 0 1 14.6 14.6 H9.4 A2.6 2.6 0 0 1 6.8 12 Z' },
  { d: 'M12 14.6 V16.4 A2.8 2.8 0 0 0 14.8 19.2 H16.4' },
]
